# Product Knowledge Model

`product_truth.json` is a fictional canonical record with a product ID, version, source reference, and attributes. `identify_claims` turns each attribute into a stable claim with a canonical value, unit, criticality, and optional concept ID. Published variants carry their own language, version, surface form, and observed value; they never overwrite Product Truth.
