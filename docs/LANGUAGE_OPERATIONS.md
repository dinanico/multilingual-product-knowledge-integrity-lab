# Language Operations

Terminology says which expression is preferred. Claim extraction recognizes which valid expression represents a concept. For example, `single-phase AC input`, `einphasigem AC-Eingang`, and `ingresso AC monofase` normalize to `input_phase_count = 1 phase`. A three-phase surface form remains `3 phase`; normalization does not repair facts.
